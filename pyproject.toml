[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM", "RET"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"schemas/*.py" = ["B008"]
